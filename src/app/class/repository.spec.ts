import { Repository } from './repository';

describe('Repository', () => {
  it('should create an instance', () => {
    expect(newRepository()).toBeTruthy();
  });
});

function newRepository(): any {
  throw new Error('Function not implemented.');
}

